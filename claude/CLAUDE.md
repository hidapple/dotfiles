# Git

- Do not run `git push` unless I explicitly tell you to push. Creating commits
  when I ask is fine, but pushing to a remote always requires my explicit
  go-ahead for that specific push — never push on your own initiative.
